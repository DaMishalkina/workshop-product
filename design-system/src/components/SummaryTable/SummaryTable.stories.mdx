import { Meta, Canvas, Story } from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";

import SummaryTable from ".";

<Meta
  title="Components/SummaryTable"
  decorators={[withDesign]}
  parameters={{
    viewport: {
      defaultViewport: "mobile1",
    },
  }}
/>

export const tableArgs = {
  items: [
    { name: "Subtotal", price: "€95.00" },
    { name: "Estimated Delivery & Handling", price: "€0.00" },
  ],
  total: "€95.00"
};

## DataTable

DataTable displays order details in a table, each row has information on name and price for each item.

<Canvas>
  <Story
  	name="Default"
  	args={tableArgs}
  	parameters={{
  		design: config({
  			type: "figma",
  			url: "https://www.figma.com/file/C3jfDdLSEM2scXfGZwk1ON/Workshop-starter-template?node-id=373%3A163",
  		})
  	}}
  >
    {(args) => <SummaryTable {...args} />}
  </Story>
</Canvas>