import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";
import BottomSwipe from ".";

<Meta
    title="Components/BottomSwipe"
    decorators={[withDesign]}
    parameters={{
        viewport: {
            defaultViewport: "mobile1",
        },
    }}
/>

## BreadCrumb

<Preview>
    <Story
        name="Default"
        parameters={{
            design: config({
                type: "figma",
                url:
                    "https://www.figma.com/file/6rrGp1UaCZM5wELZkK5teL/Workshop-Theme?node-id=113%3A3468",
            }),
        }}
        // Props values for the story
        args={{ label: "Swipe Up to Checkout",}}
    >
        {(args) => <BottomSwipe {...args} />}
    </Story>
</Preview>
