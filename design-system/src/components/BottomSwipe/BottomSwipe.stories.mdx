import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";
import BottomSwipe from ".";

<Meta title="Components/BottomSwipe" />

## BreadCrumb

TODO: add component description

<Preview>
    <Story
        name="Default"
        parameters={{
            // Configuration for design plugin, which allows to display linked Figma frame
            design: config({
                type: "figma",
                // replace this url with your own design file to link:
                url:
                    "https://www.figma.com/file/C3jfDdLSEM2scXfGZwk1ON/Workshop-starter-template?node-id=373%3A222",
            }),
            viewport: {
                defaultViewport: "mobile1",
            },
        }}
        // Props values for the story
        args={{ label: "Swipe Up to Checkout",}}
    >
        {(args) => <BottomSwipe {...args} />}
    </Story>
</Preview>
