
import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { config, withDesign } from "storybook-addon-designs";
import cartItem from "./item1.png";

import CartItem from ".";

<Meta
	title="Components/CartItem"
	decorators={[withDesign]}
	parameters={{
    viewport: {
      defaultViewport: "mobile1",
    },
  }}
/>

## CartItem

CartItem shows item's image, price and description in the cart

<Preview>
  <Story
  	name="Default"
  	parameters={{
      design: config({
      	type: "figma",
      	url: "https://www.figma.com/file/C3jfDdLSEM2scXfGZwk1ON/Workshop-starter-template?node-id=405%3A347",
      })
    }}
  >
    {(args) => <CartItem {...args} image={cartItem} name="Nike air force Xt48" description="Men's show, black" price="€95.00" />}
  </Story>
</Preview>
